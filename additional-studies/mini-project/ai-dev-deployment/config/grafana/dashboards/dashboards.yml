apiVersion: 1

providers:
  - name: 'AI Dashboards'
    orgId: 1
    folder: 'AI Monitoring'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30        # (옵션) 파일 변경 30초마다 재로드
    options:
      path: /etc/grafana/provisioning/dashboards_json
